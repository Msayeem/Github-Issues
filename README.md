
1️⃣ What is the difference between var, let, and const?

Ans: 1. var is hoisted, let and const is also hoisted but in temporal deadzone.

 2. multiple similer names can be set by var but multiple similer names cant be set by let and const.

 3. var and let can be reassigned but const cant be reassingned.

 4. var only maintains function scope but let and const maintains all scope.


 2️⃣ What is the spread operator (...)?
 Ans: The spread operator takes something iterable and spreads its contents.


3️⃣ What is the difference between map(), filter(), and forEach()?

Ans: map()  transforms each element and creates a new array. filter() only keep elements that pass the test. forEach performs action for each item.


4️⃣ What is an arrow function?

Ans: An arrow function is a function that is short and easier to write. Arrow function is not hoisted. Example:
const f1=(item) => console.log(item);


5️⃣ What are template literals?

Ans: template literals are a way to write multiple line of strings and it also enables to write html and variables in a string.
